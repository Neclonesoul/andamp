# Web

The standalone browser build imports local audio files and never uploads them. Browser persistence cannot match MediaStore guarantees. Web playback is a preview/fallback, not the Android source of truth.
