# MediaSession

`PlaybackService` is a Media3 `MediaLibraryService` with one ExoPlayer and one media session. System controls and future automotive clients operate on the same player state as the UI.
